# Olivia Client Dependencies
# Install system packages first (Debian/Raspberry Pi OS):
#   sudo apt install portaudio19-dev python3-dev
pydantic-settings>=2.2.0
pyaudio>=0.2.14
openwakeword>=0.6.0    # offline wake-word detection (default model: alexa)
websockets>=12.0
miniaudio>=1.55        # MP3 decoding (no audio device, no system deps)
numpy>=1.24.0
